# Root env file is intentionally minimal.
# Secrets and app config live next to each app.
#
# Setup (local):
#   cp apps/api-golang/.env.example apps/api-golang/.env.local
#   cp apps/web/.env.example apps/web/.env.local
#
# Database scripts (yarn db:migrate, db:seed) read apps/api-golang/.env.local
